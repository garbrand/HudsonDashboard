// Parse the available Hudson build data and return a `build` model

module.exports = Build;

function Build( project ) {
	if(! project) throw new Error('No project name given');
	
	var model;
	
	return model;
};